// Package mschema provides support for working with message schemas (that's right, it's a message schema package).
// Mostly, it's a wrapper around the Confluent Schema Registry API, but future versions may support other use cases.
package mschema
